# Backend Environment Example for Civic Pulse
# Copy this file to .env and adjust values.

# Application
PORT=5000
NODE_ENV=development

# MongoDB Connection (choose ONE approach)
# EITHER provide a full connection string:
# MONGODB_URI=mongodb://localhost:27017/civic-pulse

# OR granular parts (will be composed automatically if MONGODB_URI not set):
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=civic-pulse
# If using authentication:
# MONGO_USER=yourUser
# MONGO_PASS=yourPassword
# MONGO_AUTH_DB=admin

# JWT Secret (required for auth)
JWT_SECRET=change_me_dev_secret

# Optional: Increase Mongoose debug logging
# MONGOOSE_DEBUG=true
